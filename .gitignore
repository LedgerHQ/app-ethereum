# Compilation of Ledger's app
bin/
debug/
build/

# Nano NBGL reversed icon for Home Screen
glyphs/home_chain_*

# Unit tests and code coverage
tests/unit/build/
tests/unit/coverage/
tests/unit/coverage.*

# Python
venv/
*.pyc
__version__.py

.vscode
.idea

# Fuzzing (v1 — tests/fuzzing)
tests/fuzzing/corpus/
tests/fuzzing/out/
tests/fuzzing/CMakeFiles/
tests/fuzzing/macros/generated/

# Fuzzing (v2 — fuzzing)
fuzzing/build/
fuzzing/corpus/
fuzzing/out/
.fuzz-artifacts/

# Generated Absolution invariant — machine-local snapshot whose whole_values
# blob sizes depend on sanitizer / SDK rev / toolchain. The framework
# regenerates it via app-campaign.sh (or .clusterfuzzlite/build.sh) by
# bootstrapping to `.{}` and running sync_invariant. Never commit a local copy.
tests/fuzzing/invariants/fuzz_globals.zon

default.profraw
default.profdata
fuzz-*.log
crash-*
report.html

# LSP
.cache/
compile_commands.json

# Doxygen
doxygen/
